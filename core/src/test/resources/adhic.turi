print "hello world";
(
    /**
     * Alma is a tree argument function.
     *  ```
     *  alma(1,2,3)
     * ```
     * will return either anything or a number. The return type is `num|any`
     */
"dd"
    fn alma(a,b,c):num|any {
        """
This is a multi-line string
    """
        /**
         * /*  sss
      //   *       */
         */
    //    /*
        let alabama ;felhambra
        let alhambra
        let `class`
        let cliss
        let name = 1;let name = 2;
        `class` = 13;
        alma
    } +
    fn kabuki( args) {
        
    }
)