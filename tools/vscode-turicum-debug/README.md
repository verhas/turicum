# Turicum Debug

Local VS Code extension that registers the `turi` debug type and starts
`clifx/src/test/resources/debugger_dap.turi` as a Debug Adapter Protocol process.

Use this extension from the Turicum repository with:

```sh
code --extensionDevelopmentPath=$PROJECT_ROOT/tools/vscode-turicum-debug $PROJECT_ROOT
```

